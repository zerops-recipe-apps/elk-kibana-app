zerops:
  - setup: kibana
    build:
      deployFiles:
        - kibana.yml
    deploy:
      readinessCheck:
        httpGet:
          path: /
          port: 5601
    run:
      os: ubuntu
      base: ubuntu@24.04
      envVariables:
        ELASTICSEARCH_URL: http://elkstorage:9200 # Change, if your Elasticsearch runs on different hostname.
        ELASTICSEARCH_PASSWORD: ${elkstorage_password}
        PUBLIC_BASE_URL: $zeropsSubdomain
      ports:
        - port: 5601
          httpSupport: true
      prepareCommands:
        - wget -qO - https://artifacts.elastic.co/GPG-KEY-elasticsearch | sudo gpg --dearmor -o /usr/share/keyrings/elasticsearch-keyring.gpg
        - echo "deb [signed-by=/usr/share/keyrings/elasticsearch-keyring.gpg] https://artifacts.elastic.co/packages/8.x/apt stable main" | sudo tee /etc/apt/sources.list.d/elastic-8.x.list
        - sudo apt-get update && sudo apt-get install kibana=8.16.6
        - sudo apt-get autoremove && sudo apt-get clean # Make runtime image smaller.
        - echo 'NODE_OPTIONS="--max-old-space-size=2048"' | sudo tee -a /etc/default/kibana
      envReplace:
        delimiter: '%%'
        target:
          - kibana.yml
      initCommands:
        - sudo cp -f kibana.yml /etc/kibana/kibana.yml
        - sudo chown -R kibana /etc/kibana
        - sudo systemctl enable kibana
        - sudo systemctl start kibana